{
  "lexicon": 1,
  "id": "org.hypercerts.collection",
  "defs": {
    "main": {
      "type": "record",
      "description": "A collection/group of hypercerts that have a specific property.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["title", "claims", "createdAt"],
        "properties": {
          "title": {
            "type": "string",
            "description": "The title of this collection"
          },
          "shortDescription": {
            "type": "string",
            "maxLength": 3000,
            "maxGraphemes": 300,
            "description": "A short description of this collection"
          },
          "coverPhoto": {
            "type": "union",
            "refs": ["app.certified.defs#uri", "app.certified.defs#smallBlob"],
            "description": "The cover photo of this collection (either in URI format or in a blob)."
          },
          "claims": {
            "type": "array",
            "items": {
              "type": "ref",
              "ref": "com.atproto.repo.strongRef",
              "description": "A strong reference to a hypercert claim record. This claim must conform to the lexicon org.hypercerts.claim"
            }
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "Client-declared timestamp when this record was originally created"
          }
        }
      }
    }
  }
}